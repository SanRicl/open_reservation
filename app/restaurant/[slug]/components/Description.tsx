const Description = ({ description }: { description: string }) => {
  return (
    <div className="mt-4">
      <p className="text-lg text-justify font-light">{description}</p>
    </div>
  )
}

export default Description
